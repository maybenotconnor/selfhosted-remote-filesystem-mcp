# Remote Filesystem MCP Server - Configuration
# This file is optional - all settings have sensible defaults

# Change the port if 8080 is already in use
# PORT=8080

# Advanced: Set to true for read-only mode (no file modifications allowed)
# READ_ONLY=true