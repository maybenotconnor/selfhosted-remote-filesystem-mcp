# Remote Filesystem MCP Server Configuration
# Copy this file to .env and customize for your deployment

# ==============================================================================
# CORE CONFIGURATION
# ==============================================================================

# Allowed directories for file operations (comma-separated)
# These directories will be accessible through the MCP server
# Examples: "/data", "/home/user/documents,/home/user/projects"
ALLOWED_DIRECTORIES=/data

# Server port (default: 8080)
PORT=8080

# Server host (default: 0.0.0.0 for all interfaces)
HOST=0.0.0.0

# Enable write operations (set to false for read-only mode)
ENABLE_WRITE=true

# ==============================================================================
# AUTHENTICATION CONFIGURATION
# ==============================================================================

# Disable authentication completely (NOT RECOMMENDED for production)
# Set to "true" to disable auth (server will be publicly accessible)
DISABLE_AUTH=false

# JWT Configuration
JWT_AUDIENCE=filesystem-mcp
JWT_ISSUER=filesystem-mcp-server
JWT_ALGORITHM=RS256

# JWT Keys (optional - will be generated if not provided)
# Option 1: Provide file paths to PEM files
# JWT_PUBLIC_KEY=/path/to/public.pem
# JWT_PRIVATE_KEY=/path/to/private.pem

# Option 2: Provide keys directly (use quotes and preserve line breaks)
# JWT_PUBLIC_KEY="-----BEGIN PUBLIC KEY-----
# MIIBIjANBgkqhkiG9w0BAQEFAAOCAQ8AMIIBCgKCAQ...
# -----END PUBLIC KEY-----"

# Option 3: Leave empty to auto-generate (tokens will be printed on startup)
JWT_PUBLIC_KEY=
JWT_PRIVATE_KEY=

# ==============================================================================
# DOCKER DEPLOYMENT (when using docker-compose)
# ==============================================================================

# External port mapping (host:container)
# Change this if 8080 is already in use
# EXTERNAL_PORT=8080

# Data directory on host machine
# This will be mounted into the container
# DATA_DIR=./data

# ==============================================================================
# ADVANCED CONFIGURATION
# ==============================================================================

# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# LOG_LEVEL=INFO

# Maximum file size for read operations (in MB)
# MAX_FILE_SIZE=100

# Enable CORS (for web-based MCP clients)
# ENABLE_CORS=true

# CORS allowed origins (comma-separated)
# CORS_ORIGINS=http://localhost:3000,https://myapp.com